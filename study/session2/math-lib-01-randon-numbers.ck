
// random number generator

//infinite loop
while ( true )
{
	// generates random int
	// Math.random() => int i;
	// While this random2 method adds min and max values
	Math.random2(15, 20) => int i;

	//print value of i to console
	<<< i >>>;

	//new number every half a second
	.5::second => now; 
}

// other examples include
// Math.randomf() for float values between zero and one
// Math.random2f(-1.67, 3.45) for float values within a range
// in this case -1.67 and 3.45